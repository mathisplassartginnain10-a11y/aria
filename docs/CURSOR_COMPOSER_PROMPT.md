# Cursor Composer Prompt

Build a pixel-perfect Electron + React + TypeScript + FastAPI application matching the supplied ARIA mockup. Use Liquid Glass, animated HUD orb, three-column layout, dark premium theme, responsive desktop behavior, and production-ready architecture.